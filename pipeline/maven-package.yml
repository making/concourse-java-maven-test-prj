platform: linux

image: docker:///maven#3.3.3

inputs:
  - name: git-repo

outputs:
  - name: target

run:
  path: git-repo/pipeline/mvn.sh
  # path: mvn
  # args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]

