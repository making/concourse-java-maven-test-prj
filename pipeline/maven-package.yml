platform: linux

image: docker:///maven#3.3.3

inputs:
  - name: git-repo

outputs:
  - name: git-repo
    path: git-repo

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]

