platform: linux

image: docker:///maven#3.3.3

inputs:
  - name: git-repo

outputs:
  - name: target

run:
  path: mkdir
  args: ["-p", "target/foo/bar"]
  # path: mvn
  # args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]

